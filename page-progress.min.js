var scrollPercent = 0;var scrollPixels;$(document).scroll(function(){$.doTimeout('scroll', 250, function(){var scrollPercent = 100 * $(window).scrollTop() / ($(document).height() - $(window).height());$('.ppProgressBar').width(scrollPercent+'%');var scrollPixels = $(window).scrollTop();location.hash = scrollPixels;});});$(document).ready(function(){var ppBookmark = window.location.hash.substring(1);$(window).scrollTop(ppBookmark);});